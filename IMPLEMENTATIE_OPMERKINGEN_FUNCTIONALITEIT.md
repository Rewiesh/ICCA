# Implementatieplan: Opmerkingen Functionaliteit

**Datum:** 25 januari 2026  
**Laatst bijgewerkt:** 25 januari 2026 - 11:54  
**Gekozen oplossing:** Optie 1 - Nieuwe `icca_form_remarks` tabel  
**Status:** ✅ **KERN IMPLEMENTATIE VOLTOOID**

---

## 📊 Status Overzicht

### ✅ Voltooid (25 jan 2026)

#### Backend (ICCA)
- ✅ **Database tabel** `icca_form_remarks.tab` aangemaakt
- ✅ **Audit trigger** `icca_fmr_biur.trg` voor automatische audit velden
- ✅ **API Package Spec** `icca_audit_post_api.pks` uitgebreid met `t_remark` en `tt_remarks` types
- ✅ **API Package Body** `icca_audit_post_api.pkb`:
  - Nieuwe functie `f_get_form_remarks` voor JSON parsing
  - Nieuwe procedure `p_create_form_remarks` voor insert/update
  - Update `f_get_forms` om remarks te parsen
  - Update `p_create_audit_forms` om `p_create_form_remarks` aan te roepen
  - Update `p_dbms_output_record` voor debug output van remarks

#### Mobile App (QualityTesting)
- ✅ **Database schema** `tb_remark` tabel aangemaakt in `database1.js`
- ✅ **CRUD functies** in `database1.js`:
  - `getAllRemarksByFormId`
  - `insertRemark` / `updateRemark` / `saveFormRemark`
  - `deleteRemark`
- ✅ **Nieuw scherm** `AuditRemarkForm/index.js` voor toevoegen/bewerken opmerkingen
- ✅ **AuditErrorList** uitgebreid met tabs voor Fouten/Opmerkingen
- ✅ **Navigation** `FdisQuality.js` uitgebreid met "Opmerkingen Formulier" route
- ✅ **Upload logic** (25 jan 12:05):
  - `getAllForms` uitgebreid om remarks op te halen voor export
  - `getRemarksImages` functie toegevoegd (analoog aan `getErrorsImages`)
  - `AuditDetails/index.js` `uploadImagesWithProgress` update:
    - Haalt zowel error als remark images op
    - Upload alle images (errors + remarks)
    - Wijst remark image IDs toe na upload (regel 338-346)
- ✅ **UI verbeteringen** (25 jan 11:54):
  - ImagePickerModal component geïntegreerd in AuditRemarkForm (zoals fouten)
  - Foto thumbnail preview in opmerkingen lijst
  - Opmerkingen count in Formulieren Overzicht

### ⏳ Nog Te Doen

#### Backend (ICCA)
- ⏳ **Deployment:**
  - Compile `icca_audit_post_api` package in Oracle
  - Run DDL scripts voor `icca_form_remarks.tab` en `icca_fmr_biur.trg`
  - Test API endpoint met remarks JSON payload
- ⏳ **PDF Rapportage:** (door gebruiker zelf)
  - Aparte sectie "Algemene Opmerkingen" in PDF
  - Query remarks per form voor rapportage

#### Mobile App (QualityTesting)
- ⏳ **Testing:**
  - Test lokaal CRUD operaties (create, read, update, delete)
  - Test tab switching tussen Fouten/Opmerkingen
  - Test foto upload voor opmerkingen
- ⏳ **Upload/Sync:**
  - Test upload naar backend met remarks
  - Verify image ID assignment na upload
  - Test offline sync scenario's
- ⏳ **Deployment:**
  - Build nieuwe app versie
  - Distribueer via bestaande kanalen

#### End-to-End
- ⏳ **Integratie test:**
  - Opmerking aanmaken in mobile app
  - Upload audit naar backend
  - Verify data in `icca_form_remarks` tabel
  - Verify foto opslag in `icca_documents`

### 🚫 Out of Scope (door gebruiker)
- PDF rapportage implementatie

---

## 📋 Overzicht

### Huidige Situatie

#### Mobile App (QualityTesting)
- **Scherm:** `AuditErrorForm` (navigation naam: "Opmerkingen")
- **Database tabel:** `tb_error` met verplichte velden `ElementTypeId` en `ErrorTypeId`
- **Flow:** Fouten Lijst → + button → Opmerkingen screen
- **Probleem:** Geen mogelijkheid om algemene opmerkingen toe te voegen zonder element/fouttype

#### Backend (ICCA)
- **Tabel:** `icca_fom_errors` met NOT NULL constraints op `ete_id` en `epe_id`
- **API:** `icca_audit_post_api` verwacht `element_type_id` en `error_type_id` per error
- **PDF rapportage:** Gebruikt `icca_fom_errors` voor foutweergave

### Doel
Voeg functionaliteit toe voor **algemene opmerkingen** (met foto) zonder verplichte element/fouttype selectie, zoals in de oude app via `AuditRemarks` tabel.

---

## 🎯 Implementatie Strategie - Optie 1

Nieuwe aparte tabel `icca_form_remarks` voor opmerkingen (clean separation).

### Voordelen
- ✅ **Semantisch correct:** fouten ≠ opmerkingen
- ✅ **Geen impact op bestaande validatie:** `icca_fom_errors` blijft ongewijzigd
- ✅ **Makkelijker rapportage:** aparte queries voor fouten vs opmerkingen
- ✅ **Schaalbaar:** toekomstige uitbreidingen (bijv. opmerkingen op audit-niveau) zijn eenvoudiger

---

## 🔧 Backend Wijzigingen (ICCA)

### 1. Database Schema

#### 1.1 Nieuwe tabel: `icca_form_remarks.tab`
**Bestand:** `c:\Users\rramcharan\Documents\Q Projecten\Prd\ICCA\database\tables\icca_form_remarks.tab`

```sql
create table icca_form_remarks(
        -- fmr
        id                  number        generated by default on null as identity  not null
    ,   fom_id              number                                                  not null
    ,   remark_text         varchar2(4000)
    ,   remark_image_id     number
    ,   migrated_data       varchar2(1 char) default 'N'                            not null
    ,   created_date        date          default sysdate                           not null
    ,   created_by          varchar2(50)                                            not null
    ,   modified_date       date
    ,   modified_by         varchar2(50)
);

alter table icca_form_remarks add constraint icca_fmr_pk primary key (id);
alter table icca_form_remarks add constraint icca_fmr_fom_fk1 foreign key(fom_id) references icca_adt_forms(id);
alter table icca_form_remarks add constraint icca_fmr_doc_fk2 foreign key(remark_image_id) references icca_documents(id);

create index icca_fmr_fom_id_idx on icca_form_remarks(fom_id);
```

**Uitleg velden:**
- `fom_id`: koppeling naar formulier (`icca_adt_forms`)
- `remark_text`: opmerkingtekst (max 4000 chars)
- `remark_image_id`: optionele foto (FK naar `icca_documents`)

---

### 2. API Package Uitbreiding

#### 2.1 Package Spec: `icca_audit_post_api.pks`
**Bestand:** `c:\Users\rramcharan\Documents\Q Projecten\Prd\ICCA\database\packages\icca_audit_post_api.pks`

**Toevoegen na regel 28 (na `tt_error` definitie):**

```sql
    -- audit form remarks (nieuw)
    type t_remark is record
        (   remark_text     varchar2(4000)
        ,   remark_image_id number
        );
    type tt_remarks is table of t_remark index by pls_integer;
```

**Aanpassen `t_form` record (regel 32-41):**

```sql
    -- audit form
    type t_form is record
        (   id                  number
        ,   floor_id            number
        ,   category_id         number
        ,   form_date           date
        ,   area_code           varchar2(100)
        ,   counter_elements    number
        ,   remarks             varchar2(4000)
        ,   error               tt_error
        ,   remark              tt_remarks    -- NIEUW: opmerkingen array
        );
```

#### 2.2 Package Body: `icca_audit_post_api.pkb`

##### Wijziging A: Nieuwe procedure `p_create_form_remarks`
**Bestand:** `c:\Users\rramcharan\Documents\Q Projecten\Prd\ICCA\database\package_bodies\icca_audit_post_api.pkb`

**Toevoegen na regel 513 (na `p_create_audit_forms` procedure):**

```sql
    -----------------------------------------------------------------------------------------
    --  
    procedure p_create_form_remarks(   p_fom_id    in number
                                   ,   p_remarks   in tt_remarks
                                   )
    is
    begin
        --
        -- verwijder bestaande remarks voor deze form
        delete from icca_form_remarks
        where fom_id = p_fom_id;
        --
        -- insert nieuwe remarks
        for i in 1..p_remarks.count
        loop
            insert into icca_form_remarks (
                fom_id
            ,   remark_text
            ,   remark_image_id
            ,   created_by
            ,   modified_by
            )
            values (
                p_fom_id
            ,   p_remarks(i).remark_text
            ,   p_remarks(i).remark_image_id
            ,   'mobile_app'
            ,   'mobile_app'
            );
        end loop;
        --
    end p_create_form_remarks;
    --
```

##### Wijziging B: Aanroep in `p_create_audit_forms`
**In bestand:** `icca_audit_post_api.pkb`

**Toevoegen VOOR regel 505 (`-- update de form met het totaal aantal errors`):**

```sql
            --
            -- opslaan van opmerkingen voor deze form (indien aanwezig)
            if p_forms(i).remark.count > 0 then
                p_create_form_remarks(
                    p_fom_id    => lr_fom.id
                ,   p_remarks   => p_forms(i).remark
                );
            end if;
```

##### Wijziging C: Debug output uitbreiden
**In functie `p_dbms_output_record` (rond regel 260):**

**Toevoegen na de errors loop (na regel 259):**

```sql
                -- Nested remarks in the form (nieuw)
                if p_audit.forms(i).remark.count > 0 
                then
                    for k in 1 .. p_audit.forms(i).remark.count 
                    loop
                        dbms_output.put_line('  ↳ remark('||k||').remark_text    : '||p_audit.forms(i).remark(k).remark_text);
                        dbms_output.put_line('  ↳ remark('||k||').remark_image_id: '||p_audit.forms(i).remark(k).remark_image_id);
                    end loop;
                else
                    dbms_output.put_line('  ↳ remarks           : []');
                end if;
```

---

### 3. PDF Rapportage Uitbreiding

#### 3.1 Packages om aan te passen

**Bestanden:**
- `icca_pdf_icca_data.pkb`
- `icca_pdf_icca_zonder_cijfers_data.pkb`
- `icca_pdf_fase_control_data.pkb`
- `icca_pdf_buro_hennie_dekker_data.pkb`

**Actie per package:**
Voeg een nieuwe sectie toe om opmerkingen weer te geven (analoog aan hoe `log_book_remark` nu wordt getoond).

**Voorbeeld cursor (toevoegen aan relevante functies):**

```sql
-- cursor voor form opmerkingen
cursor c_get_form_remarks( b_adt_id in number )
is
    select  flr.name || '-' || ara.abbreviation || '.' || fom.area_number as locatie
    ,       fmr.remark_text as opmerking
    ,       fmr.remark_image_id as foto_id
    from    icca_form_remarks fmr
    join    icca_adt_forms fom on fom.id = fmr.fom_id
    join    icca_floors flr on fom.flr_id = flr.id
    join    icca_areas ara on fom.ara_id = ara.id
    where   fom.adt_id = b_adt_id
    and     fmr.remark_text is not null
    order by locatie
    ;
```

**Output in PDF:** Aparte sectie "Algemene Opmerkingen" met locatie, tekst en foto.

---

## 📱 Mobile App Wijzigingen (QualityTesting)

### 1. Database Schema

#### 1.1 Nieuwe tabel: `tb_remark`
**Bestand:** `c:\Users\rramcharan\Documents\Q Projecten\Prd\QualityTesting\app\services\database\database1.js`

**In `createTables` array (rond regel 286):**

```javascript
    `CREATE TABLE IF NOT EXISTS tb_remark (
        RemarkId INTEGER PRIMARY KEY AUTOINCREMENT,
        FormId VARCHAR NOT NULL,
        RemarkText TEXT,
        RemarkImg TEXT,
        RemarkImgSent INTEGER DEFAULT 0
    );`,
```

#### 1.2 CRUD functies voor `tb_remark`

**Toevoegen in database1.js:**

```javascript
// === REMARKS ===

async function getAllRemarksByFormId(FormId) {
  const query = "SELECT * FROM tb_remark WHERE FormId = ? ORDER BY RemarkId DESC";
  const params = [FormId];
  
  try {
    const remarks = await executeSelect(query, params);
    console.log("Remarks for FormId", FormId, ":", remarks);
    return remarks;
  } catch (error) {
    console.error("Error fetching remarks for FormId:", FormId, error);
    throw error;
  }
}

async function insertRemark(remark, FormId) {
  const query = `INSERT INTO tb_remark (FormId, RemarkText, RemarkImg, RemarkImgSent)
                 VALUES (?, ?, ?, 0)`;
  const params = [FormId, remark.RemarkText || '', remark.RemarkImg || ''];
  
  await executeTransaction(async tx => {
    const result = await tx.executeSql(query, params);
    console.log('Remark inserted successfully:', result);
  });
}

async function updateRemark(remark, FormId) {
  const query = `UPDATE tb_remark 
                 SET RemarkText = ?, RemarkImg = ?, RemarkImgSent = ?
                 WHERE RemarkId = ?`;
  const params = [
    remark.RemarkText || '',
    remark.RemarkImg || '',
    0,
    remark.RemarkId
  ];
  
  await executeTransaction(async tx => {
    await tx.executeSql(query, params);
  });
  console.log('Remark updated successfully');
}

async function saveRemark(remark, FormId) {
  if (!remark.RemarkId) {
    await insertRemark(remark, FormId);
  } else {
    await updateRemark(remark, FormId);
  }
  return remark.RemarkId;
}

async function deleteRemark(remark) {
  const query = "DELETE FROM tb_remark WHERE RemarkId = ?";
  const params = [remark.RemarkId];
  
  await executeTransaction(async tx => {
    await tx.executeSql(query, params);
  });
  console.log('Remark deleted successfully');
}
```

**Exporteren (toevoegen aan module.exports rond regel 2130):**

```javascript
  // Remarks
  getAllRemarksByFormId,
  saveRemark,
  deleteRemark,
```

---

### 2. UI Components

#### 2.1 Nieuwe screen: `AuditRemarkForm`
**Bestand:** `c:\Users\rramcharan\Documents\Q Projecten\Prd\QualityTesting\app\screens\AuditRemarkForm\index.js`

**Structuur:**
- Info text: "Voeg een algemene opmerking toe zonder foutregistratie"
- Tekst input (multiline) voor opmerking
- Foto sectie met camera knop
- Opslaan knop

**Gebaseerd op:** `AuditErrorForm` maar **zonder** ElementPicker, ErrorTypePicker, ErrorCounter

```javascript
/* eslint-disable prettier/prettier */
import React, { useState, useCallback } from 'react';
import { Alert } from 'react-native';
import {
  Box,
  ScrollView,
  Button,
  Icon,
  Text,
  TextArea,
  VStack,
  HStack,
  Pressable,
  useColorModeValue,
  useTheme,
} from 'native-base';
import MaterialIcons from 'react-native-vector-icons/MaterialIcons';
import * as database from '../../services/database/database1';
import { ImagePickerModal } from '../AuditErrorForm/components';

const AuditRemarkForm = ({ navigation, route }) => {
  const theme = useTheme();
  const [remark, setRemark] = useState(route.params?.remark || {});
  const [modalVisible, setModalVisible] = useState(false);

  const bgMain = useColorModeValue('coolGray.100', 'gray.900');
  const cardBg = useColorModeValue('white', 'gray.800');

  const onRemarkTextChange = useCallback((value) => {
    setRemark(prev => ({ ...prev, RemarkText: value }));
  }, []);

  const onSaveRemarkImage = useCallback((imageUri) => {
    setRemark(prev => ({ ...prev, RemarkImg: imageUri }));
  }, []);

  const onDeleteRemarkImage = useCallback(() => {
    setRemark(prev => ({ ...prev, RemarkImg: '' }));
  }, []);

  const saveRemark = useCallback(async () => {
    if (!remark.RemarkText?.trim() && !remark.RemarkImg) {
      Alert.alert('Fout', 'Voeg minimaal een opmerking of foto toe');
      return;
    }

    try {
      await database.saveRemark(remark, route.params.form.FormId);
      await database.setAuditUnsaved(route.params.AuditId, true);
      navigation.goBack();
    } catch (ex) {
      console.error('Error during save:', ex);
      Alert.alert('Fout', ex.message);
    }
  }, [remark, route.params.form.FormId, route.params.AuditId, navigation]);

  return (
    <Box flex={1} bg={bgMain}>
      <ScrollView flex={1} _contentContainerStyle={{ p: '4', pb: '4' }}>
        {/* Info Text */}
        <Box bg="blue.50" p="3" rounded="xl" mb="4">
          <Text fontSize="sm" color="blue.700" textAlign="center">
            Voeg een algemene opmerking toe zonder foutregistratie
          </Text>
        </Box>

        {/* Opmerking Text Input */}
        <VStack bg={cardBg} p="4" rounded="2xl" shadow={1} mb="4" space={2}>
          <HStack alignItems="center" space={2} mb="2">
            <Icon as={MaterialIcons} name="edit-note" size="md" color="purple.500" />
            <Text fontSize="md" fontWeight="bold" color="coolGray.800">
              Opmerking
            </Text>
          </HStack>
          <TextArea
            placeholder="Plaats hier uw opmerking..."
            value={remark.RemarkText || ''}
            onChangeText={onRemarkTextChange}
            autoCompleteType="off"
            h={32}
            fontSize="md"
          />
        </VStack>

        {/* Foto Sectie */}
        <VStack bg={cardBg} p="4" rounded="2xl" shadow={1} space={3}>
          <HStack alignItems="center" space={2}>
            <Icon as={MaterialIcons} name="photo-camera" size="md" color="teal.500" />
            <Text fontSize="md" fontWeight="bold" color="coolGray.800">
              Foto's
            </Text>
          </HStack>
          
          <Pressable
            onPress={() => setModalVisible(true)}
            bg="gray.50"
            p="6"
            rounded="xl"
            borderWidth={2}
            borderColor="gray.200"
            borderStyle="dashed"
          >
            <VStack alignItems="center" space={2}>
              <Icon as={MaterialIcons} name="add-a-photo" size="2xl" color="blue.400" />
              <Text fontSize="sm" color="gray.600">
                Tik om foto toe te voegen
              </Text>
            </VStack>
          </Pressable>

          {remark.RemarkImg && (
            <Box position="relative">
              <Text fontSize="sm" color="green.600">✓ Foto toegevoegd</Text>
            </Box>
          )}
        </VStack>
      </ScrollView>

      {/* Save Button */}
      <Box px="4" py="3" pb="6" bg={bgMain} shadow={3}>
        <Button
          size="lg"
          bg="fdis.500"
          _pressed={{ bg: 'fdis.600' }}
          _text={{ color: 'white', fontWeight: 'bold' }}
          rounded="xl"
          leftIcon={<Icon as={MaterialIcons} name="save" size="md" color="white" />}
          onPress={saveRemark}
        >
          Opmerking opslaan
        </Button>
      </Box>

      {/* Image Picker Modal */}
      <ImagePickerModal
        isOpen={modalVisible}
        onClose={() => setModalVisible(false)}
        title="Opmerking Foto"
        currentImage={remark.RemarkImg}
        onSaveImage={onSaveRemarkImage}
        onDeleteImage={onDeleteRemarkImage}
        onSaveError={saveRemark}
        iconColor="teal"
      />
    </Box>
  );
};

export default AuditRemarkForm;
```

---

#### 2.2 Update `AuditErrorList` met tabs

**Bestand:** `c:\Users\rramcharan\Documents\Q Projecten\Prd\QualityTesting\app\screens\AuditErrorList\index.js`

**Wijzigingen:**

1. **State toevoegen voor tab selectie (na regel 24):**
```javascript
  const [activeTab, setActiveTab] = useState('fouten'); // 'fouten' of 'opmerkingen'
  const [remarks, setRemarks] = useState([]);
```

2. **Load functie voor opmerkingen (na regel 45):**
```javascript
  const loadRemarks = useCallback(async () => {
    try {
      const fetchedRemarks = await database.getAllRemarksByFormId(form.FormId);
      setRemarks(fetchedRemarks || []);
    } catch (error) {
      console.error('Error loading remarks:', error);
    }
  }, [form.FormId]);
```

3. **Update useEffect (regel 31-36):**
```javascript
  useEffect(() => {
    if (isFocused) {
      renderAddButton();
      if (activeTab === 'fouten') {
        loadErrors();
      } else {
        loadRemarks();
      }
    }
  }, [isFocused, form.FormId, activeTab]);
```

4. **Tab Toggle Component (voor `return`, na regel 190):**
```javascript
  const renderTabToggle = () => (
    <HStack bg={cardBg} mx="4" mt="3" mb="2" p="1" rounded="xl" shadow={1}>
      <Pressable
        flex={1}
        onPress={() => setActiveTab('fouten')}
        bg={activeTab === 'fouten' ? 'fdis.500' : 'transparent'}
        py="2"
        rounded="lg"
      >
        <Text
          textAlign="center"
          fontSize="sm"
          fontWeight="bold"
          color={activeTab === 'fouten' ? 'white' : 'gray.600'}
        >
          Fouten
        </Text>
      </Pressable>
      <Pressable
        flex={1}
        onPress={() => setActiveTab('opmerkingen')}
        bg={activeTab === 'opmerkingen' ? 'fdis.500' : 'transparent'}
        py="2"
        rounded="lg"
      >
        <Text
          textAlign="center"
          fontSize="sm"
          fontWeight="bold"
          color={activeTab === 'opmerkingen' ? 'white' : 'gray.600'}
        >
          Opmerkingen
        </Text>
      </Pressable>
    </HStack>
  );
```

5. **Remark row renderer:**
```javascript
  const renderRemarkRow = useCallback(({ item }) => (
    <Pressable onPress={() => editRemark(item)}>
      {({ isPressed }) => (
        <Box
          bg={cardBg}
          mx="4"
          my="1.5"
          rounded="2xl"
          shadow={1}
          overflow="hidden"
          style={{ transform: [{ scale: isPressed ? 0.98 : 1 }] }}
        >
          <HStack alignItems="center" p="4">
            <Center bg="blue.100" size="12" rounded="xl" mr="3">
              <Icon as={MaterialIcons} name="comment" size="md" color="blue.600" />
            </Center>
            <VStack flex={1}>
              <Text fontSize="sm" color="gray.800" numberOfLines={2}>
                {item.RemarkText || 'Geen tekst'}
              </Text>
              {item.RemarkImg && (
                <HStack alignItems="center" space={1} mt="1">
                  <Icon as={MaterialIcons} name="camera-alt" size="xs" color="teal.500" />
                  <Text fontSize="xs" color="teal.500">Met foto</Text>
                </HStack>
              )}
            </VStack>
            <HStack space={2}>
              <Pressable onPress={() => deleteRemark(item)}>
                {({ isPressed: delPressed }) => (
                  <Center bg={delPressed ? 'red.100' : 'red.50'} size="10" rounded="xl">
                    <Icon as={MaterialIcons} name="delete" size="sm" color="red.500" />
                  </Center>
                )}
              </Pressable>
              <Center bg="gray.100" size="10" rounded="xl">
                <Icon as={MaterialIcons} name="chevron-right" size="sm" color="gray.400" />
              </Center>
            </HStack>
          </HStack>
        </Box>
      )}
    </Pressable>
  ), [cardBg, editRemark, deleteRemark]);
```

6. **Edit/Add/Delete handlers:**
```javascript
  const editRemark = useCallback((remark) => {
    navigation.navigate('OpmerkingenForm', { remark, form, AuditId: route.params.AuditId });
  }, [navigation, form, route.params.AuditId]);

  const addRemark = useCallback(() => {
    navigation.navigate('OpmerkingenForm', { form, AuditId: route.params.AuditId });
  }, [navigation, form, route.params.AuditId]);

  const deleteRemark = useCallback((remark) => {
    Alert.alert(
      'Bevestig Verwijdering',
      'Weet u zeker dat u deze opmerking wilt verwijderen?',
      [
        { text: 'Annuleren', style: 'cancel' },
        {
          text: 'Verwijderen',
          style: 'destructive',
          onPress: async () => {
            try {
              setRemarks(prev => prev.filter(r => r.RemarkId !== remark.RemarkId));
              await database.deleteRemark(remark);
              ShowToast({
                status: 'success',
                message: 'Opmerking succesvol verwijderd.',
                bgColor: bgMain,
                textColor: textColor,
              });
            } catch (err) {
              console.error('Error deleting remark:', err);
            }
          },
        },
      ],
      { cancelable: false }
    );
  }, [bgMain, textColor]);
```

7. **Update addError functie (regel 86-88):**
```javascript
  const addError = useCallback(() => {
    if (activeTab === 'fouten') {
      navigation.navigate('Opmerkingen', { form, AuditId: route.params.AuditId });
    } else {
      addRemark();
    }
  }, [navigation, form, route.params.AuditId, activeTab, addRemark]);
```

8. **Update empty state:**
```javascript
  const renderEmptyState = useCallback(() => {
    const isRemarksTab = activeTab === 'opmerkingen';
    return (
      <Center flex={1} py="20">
        <Center bg={isRemarksTab ? "blue.100" : "green.100"} size="20" rounded="full" mb="4">
          <Icon 
            as={MaterialIcons} 
            name={isRemarksTab ? "check-circle" : "check-circle"} 
            size="4xl" 
            color={isRemarksTab ? "blue.500" : "green.500"} 
          />
        </Center>
        <Text fontSize="lg" fontWeight="bold" color="coolGray.700" mb="1">
          {isRemarksTab ? 'Geen opmerkingen' : 'Geen fouten gevonden'}
        </Text>
        <Text fontSize="sm" color="gray.500" textAlign="center" px="8" mb="4">
          {isRemarksTab 
            ? 'Er zijn nog geen opmerkingen voor dit formulier.'
            : 'Er zijn nog geen fouten geregistreerd voor dit formulier.'}
        </Text>
        <Button
          bg="fdis.500"
          _pressed={{ bg: 'fdis.600' }}
          rounded="xl"
          leftIcon={<Icon as={MaterialIcons} name={isRemarksTab ? "add-comment" : "add"} size="sm" color="white" />}
          onPress={isRemarksTab ? addRemark : addError}
        >
          {isRemarksTab ? 'Opmerking Toevoegen' : 'Fout Toevoegen'}
        </Button>
      </Center>
    );
  }, [activeTab, addError, addRemark]);
```

9. **Update header (regel 175-188):**
```javascript
  const renderHeader = useCallback(() => {
    const count = activeTab === 'fouten' ? errors.length : remarks.length;
    const label = activeTab === 'fouten' ? 'Geregistreerde fouten' : 'Geregistreerde opmerkingen';
    const badgeColor = activeTab === 'fouten' ? 'red' : 'blue';
    
    return count > 0 ? (
      <HStack px="4" py="3" alignItems="center" justifyContent="space-between">
        <Text fontSize="sm" color="gray.500">{label}</Text>
        <Box bg={`${badgeColor}.100`} px="3" py="1" rounded="full">
          <Text fontSize="xs" fontWeight="bold" color={`${badgeColor}.600`}>
            {count} {activeTab === 'fouten' ? 'fout(en)' : 'opmerking(en)'}
          </Text>
        </Box>
      </HStack>
    ) : null;
  }, [activeTab, errors.length, remarks.length]);
```

10. **Update return statement (regel 190-205):**
```javascript
  return (
    <Box flex={1} bg={bgMain}>
      {renderTabToggle()}
      <FlatList
        data={activeTab === 'fouten' ? errors : remarks}
        renderItem={activeTab === 'fouten' ? renderErrorRow : renderRemarkRow}
        keyExtractor={item => (activeTab === 'fouten' ? item.ErrorId : item.RemarkId).toString()}
        ListHeaderComponent={renderHeader}
        ListEmptyComponent={renderEmptyState}
        contentContainerStyle={{
          flexGrow: 1,
          paddingBottom: 20,
        }}
        showsVerticalScrollIndicator={false}
      />
    </Box>
  );
```

---

#### 2.3 Navigation Update

**Bestand:** `c:\Users\rramcharan\Documents\Q Projecten\Prd\QualityTesting\app\FdisQuality.js`

**Import toevoegen (regel 18):**
```javascript
import AuditRemarkForm from './screens/AuditRemarkForm';
```

**Screen toevoegen in AuditsStack (na regel 62):**
```javascript
      <AuditsStack.Screen name="OpmerkingenForm" component={AuditRemarkForm} />
```

---

### 3. API Export Logic

#### 3.1 Update `getAllForms` functie
**Bestand:** `c:\Users\rramcharan\Documents\Q Projecten\Prd\QualityTesting\app\services\database\database1.js`

**Aanpassen functie `getAllForms` (regel 909-941):**

```javascript
const getAllForms = async AuditId => {
  try {
    const results = await executeSelect(
      `SELECT * FROM tb_form WHERE AuditId = "${AuditId}"`,
    );
    const forms = results.map(formForServer);

    const formsWithErrorsAndRemarks = await Promise.all(
      forms.map(async form => {
        // Haal errors op
        const errors = await executeSelect(
          `SELECT * FROM tb_error WHERE FormId = "${form.Id}"`,
        );
        form.Errors = errors.map(error => ({
          ElementTypeId: error.ElementTypeId,
          ErrorTypeId: error.ErrorTypeId,
          LogBook: error.LogBook === "undefined" ? "" : error.LogBook,
          TechnicalAspects:
            error.TechnicalAspects === "undefined"
              ? ""
              : error.TechnicalAspects,
          Remark: error.Remarks === "undefined" ? "" : error.Remarks,
          Count: error.CountError,
        }));
        
        // Haal remarks op (NIEUW)
        const remarks = await executeSelect(
          `SELECT * FROM tb_remark WHERE FormId = "${form.Id}"`,
        );
        form.Remarks = remarks.map(remark => ({
          RemarkText: remark.RemarkText || "",
          RemarkImageId: null, // wordt later gevuld bij image upload
        }));
        
        return form;
      }),
    );

    return formsWithErrorsAndRemarks;
  } catch (error) {
    console.error(error);
    throw error;
  }
};
```

#### 3.2 Update image upload logic

**Bestand:** `c:\Users\rramcharan\Documents\Q Projecten\Prd\QualityTesting\app\screens\AuditDetails\index.js`

**Toevoegen in `uploadImagesWithProgress` functie:**

Nieuwe functie `getRemarksImages` in database1.js (analoog aan `getErrorsImages`):

```javascript
const getRemarksImages = async auditId => {
  const formQuery = "SELECT * FROM tb_form WHERE AuditId = ?";
  const remarkQuery = "SELECT * FROM tb_remark WHERE FormId = ?";
  const params = [auditId];

  try {
    const forms = await executeSelect(formQuery, params);
    const images = [];

    for (const form of forms) {
      const remarks = await executeSelect(remarkQuery, [form.FormId]);

      for (const remark of remarks) {
        if (remark.RemarkImg && remark.RemarkImg !== "undefined") {
          images.push({
            imageRemark: {
              MimeType: "image/png",
              Image: remark.RemarkImg,
            },
            traceImageData: {
              AuditId: auditId,
              RemarkId: remark.RemarkId,
              FormId: form.FormId,
              Field: "remark",
            },
          });
        }
      }
    }

    console.log("Retrieved remark images successfully:", images);
    return images;
  } catch (error) {
    console.error("Failed to retrieve remark images:", error);
    throw error;
  }
};
```

**In `AuditDetails/index.js` bij image upload (rond regel 273):**
```javascript
// Haal error images + remark images op
const [errorImages, remarkImages] = await Promise.all([
  database.getErrorsImages(uploadAuditId),
  database.getRemarksImages(uploadAuditId)
]);
const allImages = [...errorImages, ...remarkImages];

// Upload alle images
for (let img of allImages) {
  // ... existing upload logic
}
```

**Na upload, image IDs toewijzen aan remarks (rond regel 337):**
```javascript
          // Add image IDs to remarks
          if (uploadResults) {
            forms.forEach(form => {
              form.Remarks?.forEach(remark => {
                const imgResult = uploadResults.find(r =>
                  r.FormId === form.Id && r.Field === 'remark' && r.RemarkId === remark.RemarkId
                );
                if (imgResult?.remarkImageId) {
                  remark.RemarkImageId = imgResult.remarkImageId;
                }
              });
            });
          }
```

---

## 🧪 Testing Strategie

### Mobile App Testing

1. **Tab switching:**
   - Open Fouten Lijst scherm
   - Switch tussen "Fouten" en "Opmerkingen" tabs
   - Verify data reload per tab

2. **Opmerking aanmaken:**
   - Tap "+ Opmerking Toevoegen"
   - Voer tekst in
   - Voeg foto toe
   - Opslaan → verify in lijst

3. **Opmerking bewerken:**
   - Tap bestaande opmerking
   - Wijzig tekst/foto
   - Opslaan → verify update

4. **Opmerking verwijderen:**
   - Tap delete icon
   - Bevestig verwijdering
   - Verify uit lijst

5. **Offline sync:**
   - Maak opmerkingen offline
   - Upload audit
   - Verify opmerkingen in backend

### Backend Testing

1. **API ontvangst:**
   - Verstuur audit met `forms[].Remarks` array
   - Check `icca_form_remarks` tabel voor data
   - Verify `fom_id` koppeling

2. **PDF generatie:**
   - Genereer PDF voor audit met opmerkingen
   - Verify "Algemene Opmerkingen" sectie
   - Verify foto's worden getoond

3. **Edge cases:**
   - Form zonder opmerkingen (lege array)
   - Opmerking zonder foto
   - Opmerking zonder tekst maar met foto
   - Multiple opmerkingen per form

---

## 📦 Deployment Volgorde

### Fase 1: Backend (ICCA)
1. ✅ **Database schema:**
   - Deploy `icca_form_remarks.tab`
   - Verify tabel bestaat

2. ✅ **API package spec:**
   - Deploy `icca_audit_post_api.pks` (nieuwe types)
   - Compile package spec

3. ✅ **API package body:**
   - Deploy `icca_audit_post_api.pkb` (nieuwe procedure + calls)
   - Compile package body
   - Test met dummy JSON via ORDS endpoint

4. ✅ **PDF packages:**
   - Update `icca_pdf_icca_data.pkb`
   - Update `icca_pdf_icca_zonder_cijfers_data.pkb`
   - Update `icca_pdf_fase_control_data.pkb`
   - Update `icca_pdf_buro_hennie_dekker_data.pkb`
   - Compile alle packages
   - Test PDF generatie

### Fase 2: Mobile App (QualityTesting)
1. ✅ **Database:**
   - Update `database1.js` - schema + CRUD functies
   - Test lokale database operaties

2. ✅ **UI Components:**
   - Maak `AuditRemarkForm` screen
   - Update `AuditErrorList` met tabs
   - Update navigation in `FdisQuality.js`

3. ✅ **API integratie:**
   - Update `getAllForms` export functie
   - Update image upload logic in `AuditDetails`

4. ✅ **Testing:**
   - Build app (dev mode)
   - Test volledige flow: aanmaken → bewerken → verwijderen → uploaden
   - Verify backend data

### Fase 3: Productie Release
1. ✅ **Backend:** APEX import + compile packages
2. ✅ **Mobile App:** Build + distribute nieuwe versie
3. ✅ **Documentatie:** Update gebruikershandleiding

---

## 📝 Bestandsoverzicht

### Backend (ICCA)

| Bestand | Type | Actie |
|---------|------|-------|
| `database/tables/icca_form_remarks.tab` | Nieuw | CREATE TABLE |
| `database/packages/icca_audit_post_api.pks` | Wijzig | Add `t_remark`, `tt_remarks` types; update `t_form` |
| `database/package_bodies/icca_audit_post_api.pkb` | Wijzig | Add `p_create_form_remarks` procedure; update `p_create_audit_forms` |
| `database/package_bodies/icca_pdf_icca_data.pkb` | Wijzig | Add remarks sectie in PDF |
| `database/package_bodies/icca_pdf_icca_zonder_cijfers_data.pkb` | Wijzig | Add remarks sectie in PDF |
| `database/package_bodies/icca_pdf_fase_control_data.pkb` | Wijzig | Add remarks sectie in PDF |
| `database/package_bodies/icca_pdf_buro_hennie_dekker_data.pkb` | Wijzig | Add remarks sectie in PDF |

### Mobile App (QualityTesting)

| Bestand | Type | Actie |
|---------|------|-------|
| `app/services/database/database1.js` | Wijzig | CREATE TABLE `tb_remark`; CRUD functies |
| `app/screens/AuditRemarkForm/index.js` | Nieuw | Opmerking toevoegen screen |
| `app/screens/AuditErrorList/index.js` | Wijzig | Tab toggle; remarks lijst; handlers |
| `app/screens/AuditDetails/index.js` | Wijzig | Upload remark images |
| `app/FdisQuality.js` | Wijzig | Navigation: add OpmerkingenForm screen |

---

## ⚠️ Aandachtspunten

### Data Migratie
- **Geen actie vereist:** oude `AuditRemarks` data blijft in migratie tabel
- Nieuwe opmerkingen gaan naar `icca_form_remarks`

### Backwards Compatibility
- **Mobile app versie check:** Oudere app versies sturen geen `Remarks` array
- **API handling:** Check `if p_forms(i).remark.count > 0` voordat verwerking

### Performance
- **Batch inserts:** Bij veel opmerkingen, overweeg `FORALL` in PL/SQL
- **PDF generatie:** Aparte cursor voor opmerkingen om performance te behouden

### Security
- **SQL Injection:** gebruik bind variabelen (✓ al gedaan in database1.js)
- **XSS in PDF:** gebruik `apex_escape.html` voor remark_text in PDF output

---

## 🔄 JSON API Contract

### Request Format (Mobile → Backend)

```json
{
  "audit": { ... },
  "forms": [
    {
      "Id": 123,
      "FloorId": 1,
      "CategoryId": 2,
      "AreaCode": "SR.1",
      "CounterElements": 10,
      "Remarks": "Form level opmerking",
      "Errors": [
        {
          "ElementTypeId": 166,
          "ErrorTypeId": 45,
          "Count": 2,
          "LogBook": "...",
          "TechnicalAspects": "..."
        }
      ],
      "Remarks": [
        {
          "RemarkText": "Kabel is beschadigd, moet worden vervangen",
          "RemarkImageId": 9876
        },
        {
          "RemarkText": "Ventilatie maakt geluid",
          "RemarkImageId": 9877
        }
      ]
    }
  ]
}
```

### Backend Processing

**In `icca_audit_post_api.pkb`:**

```sql
-- Loop door forms
for i in 1..p_forms.count
loop
    -- Save form
    -- Save errors (bestaand)
    -- Save remarks (NIEUW)
    if p_forms(i).remark.count > 0 then
        p_create_form_remarks(
            p_fom_id    => lr_fom.id
        ,   p_remarks   => p_forms(i).remark
        );
    end if;
end loop;
```

---

## 📊 Impact Analyse

### Database
- **Nieuwe tabel:** `icca_form_remarks` (~50-200 records per audit geschat)
- **Storage:** tekst (max 4KB) + image FK (8 bytes)
- **Indexes:** 1 index op `fom_id`

### API
- **Request size:** +10-50KB per audit (afhankelijk van aantal opmerkingen)
- **Processing time:** +50-100ms per 10 opmerkingen

### Mobile App
- **Nieuwe screen:** `AuditRemarkForm` (~200 LOC)
- **Gewijzigde screens:** `AuditErrorList` (+150 LOC), `AuditDetails` (+50 LOC)
- **Database:** nieuwe `tb_remark` tabel (~10KB lokaal)
- **APK size:** +20-30KB (nieuwe screen)

---

## ✅ Definition of Done

- [ ] Backend tabel `icca_form_remarks` aangemaakt en werkend
- [ ] API package kan opmerkingen ontvangen en opslaan
- [ ] PDF rapportage toont opmerkingen in aparte sectie
- [ ] Mobile app kan opmerkingen aanmaken/bewerken/verwijderen
- [ ] Tab toggle werkt in Fouten Lijst scherm
- [ ] Opmerkingen worden correct geüpload naar backend
- [ ] Foto's bij opmerkingen worden correct geüpload
- [ ] Bestaande fouten-functionaliteit blijft werken
- [ ] Unit tests (indien van toepassing)
- [ ] Klant akkoord na demo

---

## 🚀 Volgende Stappen

1. **Backend implementatie starten**
   - Maak `icca_form_remarks.tab`
   - Update `icca_audit_post_api` package

2. **Mobile app implementatie**
   - Maak `AuditRemarkForm` screen
   - Update `AuditErrorList` met tabs

3. **Integratie testen**
   - End-to-end test: mobile → upload → backend → PDF

4. **Productie deployment**
   - Backend eerst (backwards compatible)
   - Mobile app update daarna

---

**Geschatte doorlooptijd:** 2-3 werkdagen  
**Complexiteit:** Gemiddeld  
**Risico:** Laag (clean separation, geen breaking changes)
